[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "agente-postop"
version = "0.1.0"
description = "Agente de voz de triaje postoperatorio — Tech Sphere Challenge 2026"
requires-python = ">=3.11,<3.12"

[tool.setuptools.packages.find]
where = ["src"]
