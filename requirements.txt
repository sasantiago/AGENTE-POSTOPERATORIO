# Servidor / orquestador
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-dotenv==1.0.1
pydantic==2.10.4
pydantic-settings==2.7.1
websockets==14.1

# LLM / STT (Groq) y Gemini como respaldo documentado
groq==0.15.0
google-generativeai==0.8.3

# Ingesta y RAG
docling==2.15.1
chromadb==0.6.3
sentence-transformers==3.3.1
rank-bm25==0.2.2

# Vault watcher
watchdog==6.0.0

# Audio / TTS
soundfile==0.12.1
numpy==2.2.1
piper-tts==1.6.0
# Voz colombiana neuronal (es-CO-SalomeNeural), sin credenciales. El mínimo NO es
# arbitrario: Microsoft rota un token de autenticación (Sec-MS-GEC) y bloquea a los
# clientes viejos — con 7.0.0 el servicio responde 403 y la síntesis falla en silencio.
# Piper se conserva como respaldo y para el modo sin conexión.
edge-tts>=7.2.8

# Utilidades
httpx==0.28.1
pandas==2.2.3
openpyxl==3.1.5
python-multipart==0.0.20

# Pruebas
pytest==8.3.4
